@model StudentManagement.Models.Course

@{
    ViewData["Title"] = "Delete Course";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg border-0 border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0">
                        <i class="fas fa-exclamation-triangle"></i> Delete Course
                    </h4>
                </div>
                <div class="card-body p-4">
                    <div class="alert alert-warning" role="alert">
                        <i class="fas fa-exclamation-circle"></i> <strong>Warning!</strong> 
                        This action cannot be undone. Please confirm you want to delete this course.
                    </div>

                    <div class="card bg-light mb-4">
                        <div class="card-body">
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <h6 class="text-muted">Course Code</h6>
                                    <p class="fs-5 fw-bold">@Model.Code</p>
                                </div>
                                <div class="col-md-6">
                                    <h6 class="text-muted">Course ID</h6>
                                    <p class="fs-5">
                                        <span class="badge bg-secondary">@Model.Id</span>
                                    </p>
                                </div>
                            </div>

                            <div class="row mb-3">
                                <div class="col-md-12">
                                    <h6 class="text-muted">Course Title</h6>
                                    <p class="fs-5">@Model.Title</p>
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-md-12">
                                    <h6 class="text-muted">Credit Hours</h6>
                                    <p class="fs-5">
                                        <span class="badge bg-info">@Model.Credits Credits</span>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn btn-secondary btn-lg">
                                <i class="fas fa-times-circle"></i> Cancel
                            </a>
                            <button type="submit" class="btn btn-danger btn-lg">
                                <i class="fas fa-trash-alt"></i> Confirm Delete
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
